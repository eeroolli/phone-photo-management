# Configuration files with personal data
config.conf
# Note: config.example.conf is tracked (template for users)

# Log files with personal data
*_log_*.csv
*.log
copy_log_*.csv
move_log_*.csv
delete_log_*.csv

# Backup/version files
*\([0-9][0-9][0-9]\)*
*.bak
*.backup

# Cursor backup files
.cursorignore
\(000\).cursorignore

# Temporary files
*.tmp
*.temp

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Personal data directories
logs/
temp/
photo_hash_state/

# Original project transfer documents (contain personal data)
PROJECT_TRANSFER_ORIGINAL.md

# Windows shortcuts and runtime artifacts at repo root
*.lnk
duplicates_to_remove.txt
cross_dup_*.log
sync_log_*.csv

# Archived tools live under old/tools/ (tracked); ignore sensitive one-off outputs there if added later
